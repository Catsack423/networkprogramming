

# TCP Chat Server Advanced 💬

โปรเจกต์ระบบ Chat Server พื้นฐานที่พัฒนาด้วยภาษา Python โดยใช้ความสามารถของ **TCP Sockets** และ **Multi-threading** เพื่อรองรับการเชื่อมต่อจาก Client หลายคนพร้อมกันในเวลาเดียว

## ✨ คุณสมบัติ (Features)
- **Multi-Client Support:** รองรับการเชื่อมต่อพร้อมกันหลายจุดโดยใช้ Threading
- **Real-time Broadcasting:** ข้อความที่ส่งจาก Client คนหนึ่งจะถูกกระจายไปยังทุกคนที่เชื่อมต่ออยู่
- **Server Logging:** ระบบบันทึกกิจกรรม (Log) ลงไฟล์ `server_log.txt` พร้อมประทับเวลา
- **Graceful Shutdown:** รองรับการปิด Server อย่างปลอดภัยด้วย `Ctrl+C` โดยไม่ทำให้ Socket ค้าง
- **Error Handling:** จัดการกรณี Client หลุดหรือปิดโปรแกรมกะทันหัน

## 📁 โครงสร้างโฟลเดอร์ (Directory Structure)

week01-tcp-chat-server-advanced/
├── server/
│   ├── server.py          # จุดเริ่มต้นการทำงานของ Server
│   ├── client_handler.py   # จัดการ Logic ของ Client แต่ละราย (Thread)
│   └── logger.py          # ฟังก์ชันสำหรับบันทึก Log
├── client/
│   └── client.py          # โปรแกรมฝั่งผู้ใช้งาน
├── config.py              # ตั้งค่า IP, Port และ Buffer Size
└── .gitignore             # ไฟล์ที่ Git จะมองข้าม (เช่น __pycache__)



## 🚀 วิธีการใช้งาน (Getting Started)

### 1. การตั้งค่า (Configuration)

ตรวจสอบไฟล์ `config.py` เพื่อตั้งค่า Host และ Port ที่ต้องการ:

```python
HOST = "127.0.0.1"
PORT = 5005

```

### 2. เริ่มการทำงานของ Server

เปิด Terminal และรันคำสั่ง:

```bash
python -m server.server

```

### 3. การเชื่อมต่อจาก Client

เปิด Terminal ใหม่ (สามารถเปิดหลายจอได้) และรันคำสั่ง:

```bash
python -m client.client

```

## 🛠 เทคนิคที่ใช้ (Technical Highlights)

* **Reusable Sockets:** ใช้ `SO_REUSEADDR` เพื่อให้สามารถ Restart server ได้ทันที
* **Daemon Threads:** ใช้ Thread แบบ Daemon เพื่อให้ Thread ลูกจบการทำงานพร้อมโปรแกรมหลัก
* **Socket Timeouts:** มีการตั้งค่า Timeout เพื่อป้องกันการ Block ของคำสั่ง `accept()` และ `recv()` ช่วยให้ดักจับ KeyboardInterrupt ได้แม่นยำ

---

```
